---
// Inline SVG ornament kit for the «Русский бренд» section.
// Inlined (not background-image) so it inherits currentColor for direction
// theming and ignores the production base-path. All stroke linework so a CSS
// `color` recolors it; `fill="currentColor"` for solid nodes.
interface Props {
  kind: 'rail' | 'rosette' | 'mark' | 'scallop';
  // unique suffix so duplicate patterns on one page don't share a DOM id
  id?: string;
  class?: string;
}
const { kind, id = 'a', class: cls = '' } = Astro.props;
const pid = `${kind}-${id}`;
---
{kind === 'rail' && (
  <svg class={cls} viewBox="0 0 56 240" preserveAspectRatio="xMidYMin" aria-hidden="true">
    <defs>
      <pattern id={pid} width="56" height="120" patternUnits="userSpaceOnUse">
        <g fill="none" stroke="currentColor" stroke-width="1.1">
          <!-- central double spine -->
          <line x1="24" y1="0" x2="24" y2="120" />
          <line x1="32" y1="0" x2="32" y2="120" />
          <!-- medallion diamond -->
          <path d="M28 38 L46 60 L28 82 L10 60 Z" />
          <path d="M28 48 L38 60 L28 72 L18 60 Z" />
          <!-- eyelet rings (lace) -->
          <circle cx="28" cy="8" r="3.2" />
          <circle cx="28" cy="112" r="3.2" />
          <circle cx="11" cy="22" r="3" />
          <circle cx="45" cy="22" r="3" />
          <circle cx="11" cy="98" r="3" />
          <circle cx="45" cy="98" r="3" />
        </g>
        <g fill="currentColor">
          <circle cx="28" cy="60" r="2.1" />
        </g>
      </pattern>
    </defs>
    <rect width="56" height="240" fill={`url(#${pid})`} />
  </svg>
)}

{kind === 'rosette' && (
  <svg class={cls} viewBox="0 0 40 40" fill="none" stroke="currentColor" stroke-width="1.2" aria-hidden="true">
    <circle cx="20" cy="20" r="13" />
    <path d="M20 4 L33 20 L20 36 L7 20 Z" />
    <path d="M20 10 L30 20 L20 30 L10 20 Z" />
    <circle cx="20" cy="20" r="2.4" fill="currentColor" stroke="none" />
  </svg>
)}

{kind === 'mark' && (
  <svg class={cls} viewBox="0 0 48 48" fill="currentColor" aria-hidden="true">
    <!-- Знак «Русский бренд»: клеймо-печать с монолитной «Р».
         Один цвет (currentColor), сильный силуэт, читается в аватарке.
         Рамка-печать + засечка-«ось» внизу как отсылка к гербовой симметрии. -->
    <rect x="4.5" y="4.5" width="39" height="39" rx="8.5" fill="none" stroke="currentColor" stroke-width="2.4" />
    <path
      fill-rule="evenodd"
      clip-rule="evenodd"
      d="M17 12.5 H32.5 a4 4 0 0 1 4 4 V23 a4 4 0 0 1 -4 4 H23 V35.5 H17 Z M25 16.5 H30.5 a1.5 1.5 0 0 1 1.5 1.5 V21 a1.5 1.5 0 0 1 -1.5 1.5 H25 Z"
    />
    <rect x="20" y="38.5" width="8" height="2.2" rx="1.1" />
  </svg>
)}

{kind === 'scallop' && (
  <svg class={cls} viewBox="0 0 48 12" preserveAspectRatio="xMidYMid" aria-hidden="true">
    <defs>
      <pattern id={pid} width="24" height="12" patternUnits="userSpaceOnUse">
        <g fill="none" stroke="currentColor" stroke-width="1">
          <path d="M0 3 a6 6 0 0 0 12 0 a6 6 0 0 0 12 0" />
          <circle cx="6" cy="9" r="1.4" />
          <circle cx="18" cy="9" r="1.4" />
        </g>
      </pattern>
    </defs>
    <rect width="48" height="12" fill={`url(#${pid})`} />
  </svg>
)}
